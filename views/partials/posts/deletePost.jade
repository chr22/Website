.posts(ng-controller="DeletePostCtrl")
	h2 {{post.title}}
	div {{post.text}}
	p Are you sure you want to delete this post?
	button.rightMarg(ng-click='DeletePost()') Yes		
	button(ng-click='back()') No thanks
